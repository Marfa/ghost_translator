services:
  ghost-translator:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      MAP_FILE: /app/data/map.json
    restart: unless-stopped
